// Default recipient — used until the technician sets a different one in Ajustes
// (stored in settings.json via lib/settings-manager.ts).
export const EMAIL_CONFIG = {
  recipientEmail: 'hector13-zalo@hotmail.com',
};

export const APP_VERSION = '1.0.0';
