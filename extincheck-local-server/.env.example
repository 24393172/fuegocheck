HOST=0.0.0.0
PORT=3001
# Relative paths are resolved from this server folder.
DATABASE_PATH=./data/extincheck-local.sqlite
# Browser origins allowed during local development. Native apps send no Origin header.
CORS_ORIGINS=http://localhost:8081,http://localhost:19006
