# EAS Build and local installs both need this: the project has a pre-existing
# react/react-dom peer-dependency conflict (react 19.1.0 pinned by Expo SDK 54
# vs transitive react-dom 19.2.x). Without it, `npm install` fails on ERESOLVE.
legacy-peer-deps=true
