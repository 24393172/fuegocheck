// Stub — not the real react-native-worklets package.
// react-native-css-interop/babel.js unconditionally requires the worklets
// Babel plugin as a string, even when worklets are not used.
// This stub satisfies that require() without registering any transform.
module.exports = {};
